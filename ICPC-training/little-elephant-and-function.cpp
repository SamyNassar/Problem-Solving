#include <bits/stdc++.h>
using namespace std;

int main()
{

    cin.tie(0);
    cout.tie(0);
    ios::sync_with_stdio(false);

    int n = 0;
    cin >> n;
    cout << n;
    for (int i = 1; i < n; i++)
    {
        cout << ' ' << i;
    }
    cout << endl;
    
    return 0;
}
